services:
  # Frontend Static Site - Standalone
  - type: web  
    name: apex-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run export
    staticPublishPath: frontend/out
    envVars:
      - key: NODE_VERSION
        value: 18.17.0 